add_subdirectory(parser)
add_subdirectory(translator)
add_subdirectory(grpc)
add_subdirectory(service)


# add_executable(er_cat main.cpp)

# target_link_libraries(er_cat PRIVATE parser utils translator)